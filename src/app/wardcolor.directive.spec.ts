import { WardcolorDirective } from './wardcolor.directive';

describe('WardcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new WardcolorDirective();
    expect(directive).toBeTruthy();
  });
});
